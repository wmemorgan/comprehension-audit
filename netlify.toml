[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/audit-submit"
  to = "/.netlify/functions/audit-submit"
  status = 200
